[project]
name = "contrastive-td"
version = "0.1.0"
description = "Contrastive learning utilities for tracksdata"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.9.0",
    "tracksdata>=0.1.0rc0",
]
